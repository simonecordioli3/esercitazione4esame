#include "most_frequent.h"

/**
 * Trova la lettera più presente nella stringa in input e ne conta le occorrenze
 * @param [in] str stringa da analizzare all'interno della funzione
 * @param [out] count numero di occorrenze del carattere più frequente
 * @return carattere più frequente all'interno della stringa
 * La stringa è composta esclusivamente da caratteri maiuscoli e spazi
 * Gli spazi possono essere scusi dal conteggio
 * Se c'è più di un carattere con la stessa frequenze, ritornare il primo in ordine alfabetico
 * Se la stringa ha lunghezza 0 restituire 'A' con count pari a 0
**/
char most_frequent(char str[], int *count) {
    return 'A'; // modificare se necessario
}